__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.pytest-*/
pytest-cache-files-*/
.pytest_tmp/
tmp/
.mypy_cache/
.ruff_cache/
.venv/
venv/
# PDFs are build artifacts - rebuild from docs/*.tex (see docs/BUILD_PDF.md)
*.pdf

# secrets - never commit an API key
.env
*.key

# Generated campaign/runtime state. Curated result corpora may still be
# committed explicitly with `git add -f` after review.
experiments/*_api_raw.jsonl
experiments/*_api_rate_state.json
experiments/*.v*.backup.json
